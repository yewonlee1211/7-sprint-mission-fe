"use client";

import CustomBtn from "@/components/CustomBtn";

export default function Home() {
  return (
    <div>
      <CustomBtn text="로그인" onClick={() => {}} />
      <CustomBtn text="구경하러 가기" onClick={() => {}} />
    </div>
  );
}
